from typing import *
from math import fsum
from pprint import pprint
from collections import OrderedDict,defaultdict
def renomen(Zeft:int,Anigma:str)->str:
    return "ewfe"
Numb={"wewef",8}
class Veridonce(object):
    def __init__(self,List:Iterable[object]=None):
     if(List==None):
         List=defaultdict(list)
         names = ('''Feruda Sedaka Landrivers Satelite Carbon Jerusalim Atomic Mason Enerji Vertical Modern Warfare'''.split())

         for name in names:
             fulter = name[0]
             List[fulter].append(name)
         pprint(List)
     else:
       for i in List:
           print(i)

Veridonce()
Nematrix=[
    (100,200,300),
    (400,604,74),
    (443,347,236)
]
Point=Tuple[int, ...]
pprint(list(zip(Nematrix)),width=40)
Point=Nematrix
pprint(Point)